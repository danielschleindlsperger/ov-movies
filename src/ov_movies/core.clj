(ns ov-movies.core
  (:gen-class)
  (:require [ov-movies.scrape :as scrape]))

;; (scrape/movies-with-original-screenings)

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello"))

(-main)