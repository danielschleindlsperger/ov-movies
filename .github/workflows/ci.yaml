name: CI

on:
  push:
    branches:
      - master
jobs:
  build:
    name: CI
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install dependencies
        run: lein deps
      - name: Run tests
        run: lein test
      - name: Lint
        run: lein kibit
